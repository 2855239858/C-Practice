#ifndef _CMAKE_PRACTICE_INCLUDE_
#define _CAMKE_PRACTICE_INCLUDE_

namespace math{
    template<typename T>
    T add(T num1, T num2);

    template<typename T>
    T multi(T num1, T num2);

    int add_add(int &num);
}

#endif