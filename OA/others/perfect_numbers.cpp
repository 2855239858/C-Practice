#include <iostream>
#include <vector>
#include <cmath>
#include <unordered_set>

using namespace std;

int perfectNumbers(int n) {
    unordered_set<int> exist;
    vector<int> pows = {1};
 
    for (int i = 2; i < n; ++i) {
        if ((long long) i * i > n) {
            break;
        }
        for (int j = 2; j < )
    }
}